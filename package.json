{
  "name": "sosovault-contracts",
  "version": "0.1.0",
  "description": "SoSoVault smart contracts (Wave 1: documented + tested, not deployed)",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sodex": "hardhat run scripts/deploy.js --network sodexTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
